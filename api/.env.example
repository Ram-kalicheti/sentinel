# copy to api/.env (gitignored) and fill the real value from the azure cache access key
# azure cache exposes only the tls port so the scheme is rediss with double s on 6380
REDIS_URL=rediss://:REPLACE_WITH_ACCESS_KEY@your-cache-name.redis.cache.windows.net:6380/0